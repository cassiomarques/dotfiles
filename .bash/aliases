# changing directory to code project
function d { cd ~/dev/$1; }

# alternative to "rails" command to use templates
function railsapp {
  template=$1
  appname=$2
  shift 2
  rails $appname -m http://github.com/ryanb/rails-templates/raw/master/$template.rb $@
}

# misc
alias vim='/Applications/Macvim.app/Contents/MacOS/Vim'
alias reload='. ~/.bash_profile'
alias cshdb="psql -U postgres -h 10.10.1.9 -d csh"
alias csldb="psql -U postgres -h 10.10.1.9 -d csl"
alias csl="cd ~/dev/shh/csl"
alias eblood="terminitor start eblood"
alias crm="cd ~/dev/surgeworks/crm"
alias ecom="cd ~/dev/surgeworks/cancao-ecom"

# git
alias gl='git log --graph --pretty=format:'\''%Cred%h%Creset -%C(yellow)%d%Creset %Cblue%an%Creset - %s %Cgreen(%cr)%Creset'\'' --abbrev-commit --date=relative'
alias ga='git add .'
alias gs='git status'
alias gp='git push'

# Rails 2
alias ss="script/server"
alias sc="script/console"
alias db="script/dbconsole"

# Rails 3
alias rc="rails console"
alias rs="rails server"
alias rdb="rails dbconsole"
alias migrate="bundle exec rake db:migrate && bundle exec rake db:test:prepare"

# Daiy Digital
alias ddstaging="ssh eyst"
alias ddstagingdev="ssh eyst-dev"
alias ddprod="ssh eyprd"
# We're using the staging env as "production" for now, and the staging_development env as "staging"...
alias stagdeploy="git push origin staging && ey deploy -e staging_development"
alias proddeploy="git push origin master && ey deploy -e staging"
alias allspecs="rspec spec/models && rspec spec/services && rspec spec/requests && rspec spec/lib && rspec spec/cells && rspec spec/helpers && rspec spec/presenters"

# Titanium 
alias titanium="/Users/cassiommc/Library/Application\ Support/Titanium/mobilesdk/osx/1.7.2/titanium.py"
